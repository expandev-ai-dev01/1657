import { Router } from 'express';

const router = Router();

// Placeholder for public routes like login, register, password-reset, etc.
// Example:
// import * as authController from '@/api/v1/external/security/auth/controller';
// router.post('/auth/login', authController.loginHandler);

export default router;
